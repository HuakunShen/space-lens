// Shared prop types for the component library. Components never import the
// transport or the app; they speak these shapes and take callbacks.
export type { CollectorEntry, ScanStatus, ScanTarget, TreeNodeSummary, TreeSliceNode } from '@space-lens/contract'
