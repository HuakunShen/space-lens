# CLI adapter crate for exercising the Rust file search core locally.
[package]
name = "kfs-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
kfs-core = { path = "../kfs-core" }
kfs-daemon = { path = "../kfs-daemon" }
kfs-index-sqlite = { path = "../kfs-index-sqlite" }
kfs-provider-spotlight = { path = "../kfs-provider-spotlight" }
kfs-watcher = { path = "../kfs-watcher" }

[[bin]]
name = "kfs"
path = "src/main.rs"

[lints]
workspace = true
