# Migration from Kunkun

This repository is the extracted file-search workspace of
[kunkunsh/kunkun](https://github.com/kunkunsh/kunkun), preserved with its full
git history.

## Source

| Fact | Value |
|---|---|
| Source repository | `kunkunsh/kunkun` (local checkout `/Users/hk/Dev/kunkun`, branch `v2`) |
| Frozen source commit | `05808499147a62adb7039944da44e8e968abc31e` |
| Subtree tree hash | `b7912b0fbabf8563b4b06ecf36d7e74879d15aa7` |
| Source path | `crates/file-search/` |
| Capability commit range | `22a27b45dcc97b91ff8b21410f04061d5a27f768` through `4014239286ac40b23f9abd186b816fd082ede739` (nine commits, 2026-06-02 to 2026-06-05) |

The history was extracted with
`git filter-repo --path crates/file-search/ --path-rename crates/file-search/:`
on a fresh `--no-local` clone, so authors, timestamps and commit messages of
the nine capability commits are retained and the extracted root tree is
byte-identical to the frozen source subtree (the tree hashes match exactly).

## Deliberately excluded (they remain in Kunkun)

Parent-only wiring never belonged to the Rust workspace and was not migrated:

- `packages/core/src/file-search/**` — shell-neutral TypeScript service
- `packages/db/helpers/file-search-roots.ts` — root/settings orchestration
- `apps/desktop/electron/file-search/**` and RPC/preload wiring
- `packages/client/src/file-search/contract.ts` and the Hono/OpenAPI routes
- the `pnpm-workspace.yaml` entry pointing at `crates/file-search/crates/kfs-napi`

## Generated files

`crates/kfs-napi/index.js` and `crates/kfs-napi/index.d.ts` are generated by
NAPI-RS (`napi build`) and stay tracked because `main`/`types` resolve to them
before any consumer build. CI regenerates both and fails on a diff; nobody
hand-edits them. Platform binaries (`*.node`) are never committed.

## Post-extraction changes (not in Kunkun history)

- `style`: one `cargo fmt` fix in `crates/kfs-napi/src/lib.rs` (the Kunkun
  checkout was shared and left untouched; the drift was formatting only).
- `feat(kfs-index-sqlite)`: monotonic `SCHEMA_VERSION` (`PRAGMA user_version`),
  refused-forward migration for newer databases, and upgrade tests. Kunkun
  databases migrate in place from version 0.
- License, CI, standalone repository metadata and this file.

## License

MIT; see `LICENSE`. The workspace metadata already declared `license = "MIT"`
before extraction, but no license text existed in Kunkun.
